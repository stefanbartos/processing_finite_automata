package fi.muni.cz.pb138.XMLData;

import java.io.File;
import java.net.URI;

/**
 * Created by laky on 21.6.16.
 */
public class XMLDataStorage {

    //TODO: HEADERS MIGHT CHANGE
    /**
     * Method to store XML files. Will be stored as files in some directory structure inside backend?
     * @param uri URI to file
     * @return true if nothing failed, false or possible exception if something does.
     */

    public boolean store(URI uri) {
        return false;
    }

    /**
     * No idea how I will implement this, however somehow it should be done QQ
     * @return
     */
    public File load(URI uri) {
        return null;
    }
}
